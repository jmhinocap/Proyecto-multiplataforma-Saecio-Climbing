spring:
  datasource:
    url: jdbc:mysql://localhost:3306/SaecioClimbingAPI
    username: root
    password: finocchio
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8086
logging:
  level:
    root: INFO
